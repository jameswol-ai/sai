apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: sai-monitoring
  namespace: flux-system
spec:
  interval: 5m
  path: ./monitoring
  prune: true
  sourceRef:
    kind: GitRepository
    name: sai-monitoring-repo
  targetNamespace: monitoring
  wait: true
