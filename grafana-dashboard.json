{
  "dashboard": {
    "id": null,
    "title": "SAI Trading Cockpit",
    "panels": [
      {
        "type": "graph",
        "title": "Live Trading Return",
        "targets": [
          { "expr": "sai_live_return", "legendFormat": "Live Return" }
        ],
        "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 }
      },
      {
        "type": "graph",
        "title": "Backtest Return",
        "targets": [
          { "expr": "sai_backtest_return", "legendFormat": "Backtest Return" }
        ],
        "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 }
      },
      {
        "type": "graph",
        "title": "Drawdown Comparison",
        "targets": [
          { "expr": "sai_live_drawdown", "legendFormat": "Live Drawdown" },
          { "expr": "sai_backtest_drawdown", "legendFormat": "Backtest Drawdown" }
        ],
        "gridPos": { "x": 0, "y": 8, "w": 24, "h": 8 }
      },
      {
        "type": "stat",
        "title": "QA Tests Passed",
        "targets": [
          { "expr": "sai_tests_passed" }
        ],
        "gridPos": { "x": 0, "y": 16, "w": 12, "h": 4 }
      },
      {
        "type": "stat",
        "title": "QA Tests Failed",
        "targets": [
          { "expr": "sai_tests_failed" }
        ],
        "gridPos": { "x": 12, "y": 16, "w": 12, "h": 4 }
      }
    ]
  }
}
        ],
        "id": 4
      },
      {
        "type": "graph",
        "title": "Win Rate",
        "targets": [
          { "expr": "sai_win_rate", "legendFormat": "Win Rate" }
        ],
        "id": 5
      },
      {
        "type": "graph",
        "title": "Sharpe Ratio",
        "targets": [
          { "expr": "sai_sharpe_ratio", "legendFormat": "Sharpe Ratio" }
        ],
        "id": 6
      }
    ],
    "schemaVersion": 36,
    "version": 1
  }
}
