# Deployment Guide

## Overview
This document provides instructions for deploying the AI Trading Bot locally, with Docker, and in the cloud.

## Local Deployment
1. Clone the repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
